# common-lib

Shared constants and DTO helpers used across services.

## Build
```
./mvnw -B test
```

## Publish (GitHub Packages)
Tag a release like `v0.1.0` to publish.

## Local install
```
./mvnw -DskipTests install
```
